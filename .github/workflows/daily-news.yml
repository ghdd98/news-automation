name: 📰 Daily News Automation

on:
  schedule:
    # 매일 오전 6시 (한국시간 = UTC 21:00 전날)
    - cron: '0 21 * * *'
  workflow_dispatch:  # 수동 실행도 가능

jobs:
  collect-news:
    runs-on: ubuntu-latest
    timeout-minutes: 90  # 최대 90분

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 📦 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📚 의존성 설치
        run: npm ci

      - name: 🔐 환경 변수 설정
        run: |
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "NAVER_CLIENT_ID=${{ secrets.NAVER_CLIENT_ID }}" >> .env
          echo "NAVER_CLIENT_SECRET=${{ secrets.NAVER_CLIENT_SECRET }}" >> .env

      - name: 🚀 뉴스 자동화 실행
        run: node src/index.js
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}

      - name: 📤 뉴스 데이터 저장 (Git Push)
        run: |
          git config --global user.name "News Bot"
          git config --global user.email "ghdd98@users.noreply.github.com"
          
          if [ -d "data" ]; then
            # 1. 변경된 파일들을 먼저 스테이징
            git add data/
            
            if ! git diff --staged --quiet; then
              # 2. 임시 커밋 생성 (rebase 전에 변경사항 보호)
              git commit -m "📰 News Update: $(TZ='Asia/Seoul' date +'%Y-%m-%d %H:%M')"
              
              # 3. 원격 저장소 최신 변경사항과 동기화
              git pull --rebase origin main
              
              # 4. 푸시
              git push
              echo "✅ 뉴스 데이터가 성공적으로 저장되었습니다."
            else
              echo "⚠️ 변경된 뉴스가 없습니다."
            fi
          else
            echo "❌ data 폴더가 생성되지 않았습니다."
            exit 1
          fi

      - name: 🌐 Vercel 대시보드 자동 업데이트
        run: |
          echo "📦 news-dashboard 저장소로 데이터 동기화 중..."
          
          # news-dashboard 리포 클론
          git clone https://x-access-token:${{ secrets.PAT_TOKEN }}@github.com/ghdd98/news-dashboard.git ../dashboard-temp
          
          # 데이터 복사
          cp -r data/* ../dashboard-temp/data/
          
          # 변경사항 커밋 및 푸시
          cd ../dashboard-temp
          git config user.name "News Bot"
          git config user.email "ghdd98@users.noreply.github.com"
          git add data/
          
          if ! git diff --staged --quiet; then
            git commit -m "📰 Auto-sync: $(date +'%Y-%m-%d %H:%M')"
            git push
            echo "✅ Vercel 대시보드가 자동으로 업데이트됩니다!"
          else
            echo "⚠️ 동기화할 새 데이터가 없습니다."
          fi
