name: 🧪 Test Category News

on:
  workflow_dispatch:  # 수동 실행만

jobs:
  test-news:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: 📥 코드 체크아웃
        uses: actions/checkout@v4

      - name: 📦 Node.js 설정
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: 📚 의존성 설치
        run: npm ci

      - name: 🔐 환경 변수 설정
        run: |
          echo "GEMINI_API_KEY=${{ secrets.GEMINI_API_KEY }}" >> .env
          echo "NAVER_CLIENT_ID=${{ secrets.NAVER_CLIENT_ID }}" >> .env
          echo "NAVER_CLIENT_SECRET=${{ secrets.NAVER_CLIENT_SECRET }}" >> .env

      - name: 🧪 테스트 뉴스 수집 실행
        run: node src/index.js
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          NAVER_CLIENT_ID: ${{ secrets.NAVER_CLIENT_ID }}
          NAVER_CLIENT_SECRET: ${{ secrets.NAVER_CLIENT_SECRET }}
          TEST_MODE: 'true'

      - name: 📤 테스트 데이터 저장 (Git Push)
        run: |
          git config --global user.name "News Bot"
          git config --global user.email "ghdd98@users.noreply.github.com"
          
          if [ -f "data/news_test.json" ]; then
            git add data/news_test.json
            git commit -m "🧪 Test: Category news collection" || echo "No changes"
            git push origin main
          else
            echo "No test data generated"
          fi
