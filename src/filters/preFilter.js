/**
 * AI 분석 전 사전 필터링
 * 제목 + 설명을 분석하여 광고성/무관 기사 제거
 */

// ==================== 제외 패턴 (대폭 확장) ====================
const EXCLUDE_PATTERNS = [
    // 광고/홍보
    /\[광고\]/i, /\[AD\]/i, /\[PR\]/i, /\[후원\]/i, /\[협찬\]/i,
    /\[제휴\]/i, /\[파트너\]/i, /sponsored/i, /advertisement/i,
    /브랜디드/, /네이티브 광고/, /제휴콘텐츠/,

    // 이벤트/프로모션/쇼핑 (구매 제외 - 기업 맥락에서 사용됨)
    /이벤트 참여/, /경품 응모/, /할인 쿠폰/, /세일 기간/, /특가 행사/, /무료체험/,
    /응모하/, /당첨자/, /추첨/, /증정/, /사은품/, /프로모션 행사/,
    /쇼핑몰/, /최저가 구매/, /핫딜/, /타임세일/,
    /블랙프라이데이/, /사이버먼데이/, /빅세일/,

    // 연예/엔터테인먼트 (연기 → 연기자/연기력으로 구체화)
    /드라마 출연/, /예능 프로그램/, /아이돌 그룹/, /걸그룹/, /보이그룹/, /가수 활동/,
    /배우 출연/, /연기자/, /연기력/, /연기 활동/, /연예인/, /셀럽/, /스타 화보/, /팬덤/,
    /콘서트 개최/, /팬미팅/, /앨범 발매/, /음원 차트/, /컴백 무대/,
    /뮤직비디오/, /뮤비 공개/, /티저 영상/, /공개방송/,
    /영화 개봉/, /박스오피스/, /관객수 돌파/,
    /웹툰 연재/, /웹소설/, /만화책/,

    // 스포츠 (선수 → 스포츠 선수로 구체화)
    /축구 경기/, /야구 경기/, /농구 경기/, /배구 경기/, /골프대회/, /테니스 대회/,
    /올림픽 출전/, /월드컵/, /프로리그/, /경기 결과/, /승리를 거/, /패배를 당/,
    /스포츠 선수/, /야구 선수/, /축구 선수/, /감독 선임/, /코치 영입/, /선수 이적/, /트레이드/,
    /e스포츠 대회/, /게임대회/, /롤드컵/, /LCK 결승/,

    // 미디어/포토/영상 (사진 → 포토 관련으로 구체화)
    /포토뉴스/, /화보 촬영/, /움짤/, /직캠 영상/, /영상뉴스/,
    /\[포토\]/, /\[영상\]/, /\[움짤\]/, /\[화보\]/,
    /포토 갤러리/, /비하인드 컷/, /메이킹 필름/,

    // 날씨/운세/라이프
    /오늘의 운세/, /오늘의 날씨/, /주간 날씨/, /일기예보/,
    /별자리 운세/, /타로 점/, /사주팔자/, /궁합 보기/,
    /맛집 추천/, /레시피 공개/, /요리 방법/, /카페 추천/, /맛집탐방/,
    /여행지 추천/, /호텔 예약/, /숙박 할인/, /관광 명소/, /휴가 계획/,
    /패션 스타일/, /뷰티 팁/, /화장품 추천/, /메이크업 튜토리얼/, /헤어 스타일/,
    /다이어트 방법/, /피트니스 팁/,

    // 부동산/개인금융 (기업 뉴스 아닌 것)
    /아파트 분양/, /청약 일정/, /전세 시세/, /월세 가격/, /부동산 매매/,
    /대출 금리 비교/, /예금 금리/, /적금 추천/, /개인 재테크/,
    /신용카드 추천/, /카드 혜택/, /포인트 적립/, /마일리지 사용/,

    // 정치/사회 일반
    /국회 본회의/, /여당 의원/, /야당 대표/, /대통령 발언/, /총리 후보/, /장관후보자/,
    /선거 유세/, /투표 결과/, /공천 경쟁/, /정당 활동/, /당대표 선출/,
    /사건사고 현장/, /범죄 수사/, /검찰 기소/, /경찰 조사/, /재판 결과/, /법원 판결/,
    /시위 현장/, /집회 참가/, /농성 돌입/, /파업 돌입/,

    // 교육/입시
    /수능 시험/, /대입 전형/, /입시 설명회/, /학원 수강/, /과외 모집/,
    /대학 합격/, /등록금 인상/, /장학금 수혜/,

    // 건강/의료 일반 (기업 의료 아닌 것)
    /건강 관리 팁/, /질병 예방/, /치료 방법/, /병원 진료/, /처방전/,
    /다이어트 식단/, /살빼기 비법/, /체중 감량/, /비만 관리/,

    // 기타 무관
    /단독 인터뷰/, /칼럼니스트/, /사설/, /기고문/, /독자투고/,
    /부고 소식/, /조의/, /별세 소식/, /타계하/,
    /복권 당첨/, /로또 번호/, /당첨번호 공개/
];

// ==================== 비즈니스 필수 키워드 (대폭 확장) ====================
const BUSINESS_KEYWORDS = [
    // === 재무/실적 ===
    '실적', '매출', '영업이익', '순이익', '당기순이익', '영업손실',
    '분기', '연간', '전년대비', '전분기', '흑자', '적자', '호실적',
    '어닝서프라이즈', '어닝쇼크', '컨센서스', '실적발표', '잠정실적',

    // === 수주/계약 ===
    '수주', '계약', '낙찰', '공급계약', '납품계약', '독점계약',
    '협약', 'MOU', '파트너십', '제휴', '협력', '조인트벤처',
    '1조', '조원', '억원', '대형계약', '메가딜', '빅딜',

    // === M&A/지배구조 ===
    '인수', '합병', 'M&A', '분할', '분사', '자회사', '계열사',
    '지분', '지분매각', '지분인수', '경영권', '최대주주',
    '상장', 'IPO', '스팩', '상장폐지', '재상장',

    // === 투자/자금 ===
    '투자', '증자', '유상증자', '무상증자', 'CB', 'BW', '전환사채',
    '배당', '배당금', '자사주', '자사주매입', '주주환원',
    '펀딩', '투자유치', '시리즈A', '시리즈B', '시리즈C',

    // === 주가/시장 ===
    '주가', '시가총액', '52주 신고가', '52주 신저가', '급등', '급락',
    '상한가', '하한가', '거래량', '외국인', '기관', '개인투자자',

    // === 사업/제품 ===
    '신사업', '신제품', '출시', '개발', '생산', '양산', '상용화',
    '공장', '설비', '증설', '라인', '생산능력', '캐파', 'CAPA',
    '수출', '수입', '판매', '납품', '공급', '물량',

    // === 인사/조직 ===
    '대표이사', 'CEO', '사장', '회장', '부회장', '부사장', '전무', '상무',
    '임원', '인사', '조직개편', '구조조정', '희망퇴직', '인력구조조정',
    '채용', '인력', '공채', '경력채용', '대규모채용',

    // === 기술/R&D ===
    '기술', '특허', '연구', 'R&D', '혁신', '개발', '연구소',
    'AI', '인공지능', '머신러닝', '딥러닝', 'GPT', 'LLM',
    '자율주행', '전기차', 'EV', '배터리', '2차전지', '전고체',
    '반도체', '파운드리', '메모리', 'HBM', 'DRAM', 'NAND',
    '5G', '6G', '통신', '네트워크', '클라우드', '데이터센터',
    '로봇', '드론', '우주', '위성', '발사',

    // === 산업/시장 ===
    '시장', '점유율', '경쟁', '업계', '산업', '섹터',
    '전망', '분석', '리포트', '보고서', '전략', '목표가',
    '1위', '2위', '3위', '선두', '후발', '추격',

    // === 글로벌 ===
    '해외진출', '수출', '글로벌', '북미', '유럽', '중국', '일본',
    '현지법인', '해외법인', '글로벌시장', '세계시장',

    // === 규제/정책 ===
    '규제', '정책', '법안', '개정', '시행', '인허가', '승인',
    '관세', '반덤핑', '무역', 'IRA', 'CHIPS', '보조금',

    // === 영문 키워드 ===
    'revenue', 'profit', 'earnings', 'quarterly', 'annual',
    'acquisition', 'merger', 'partnership', 'investment',
    'launch', 'release', 'develop', 'expand', 'growth'
];

/**
 * 광고성/무관 패턴 체크
 */
function hasExcludePattern(text) {
    return EXCLUDE_PATTERNS.some(pattern => pattern.test(text));
}

/**
 * 비즈니스 관련 키워드 체크
 */
function hasBusinessKeyword(text) {
    const lowerText = text.toLowerCase();
    return BUSINESS_KEYWORDS.some(keyword =>
        lowerText.includes(keyword.toLowerCase())
    );
}

/**
 * 설명문 품질 체크
 */
function hasQualityDescription(description) {
    if (!description) return false;
    if (description.length < 50) return false;

    const sentences = description.split(/[.!?]/);
    const validSentences = sentences.filter(s => s.trim().length > 20);

    return validSentences.length >= 1;
}

/**
 * 사전 필터링 메인 함수
 */
export function preFilterNews(newsItems) {
    const passed = [];
    const excluded = {
        adPattern: 0,
        noBusinessKeyword: 0,
        lowQuality: 0
    };

    for (const item of newsItems) {
        const fullText = `${item.title} ${item.description || ''}`;

        // 1. 광고성 패턴 체크
        if (hasExcludePattern(fullText)) {
            excluded.adPattern++;
            continue;
        }

        // 2. 글로벌 뉴스는 이미 기업명으로 검색됨 → 바로 통과
        if (item.isGlobal) {
            passed.push(item);
            continue;
        }

        // 3. 비즈니스 키워드 체크 (제목 또는 설명에 포함)
        const hasBusinessContent = hasBusinessKeyword(fullText);

        // 4. 기업명이 이미 매칭된 경우 비즈니스 키워드 없어도 통과
        const hasCompany = item.companies && item.companies.length > 0;

        if (!hasBusinessContent && !hasCompany) {
            excluded.noBusinessKeyword++;
            continue;
        }

        // 5. 설명문 품질 체크 (있는 경우만)
        if (item.description && !hasQualityDescription(item.description)) {
            if (hasCompany && hasBusinessKeyword(item.title)) {
                passed.push(item);
                continue;
            }
            excluded.lowQuality++;
            continue;
        }

        passed.push(item);
    }

    const globalCount = passed.filter(n => n.isGlobal).length;
    const domesticCount = passed.length - globalCount;

    console.log(`🎯 [사전 필터] ${newsItems.length}개 → ${passed.length}개 통과`);
    console.log(`   ├─ 광고/무관 제외: ${excluded.adPattern}개`);
    console.log(`   ├─ 비즈니스 무관: ${excluded.noBusinessKeyword}개`);
    console.log(`   ├─ 저품질: ${excluded.lowQuality}개`);
    console.log(`   └─ 통과 (국내: ${domesticCount}, 해외: ${globalCount})`);

    return passed;
}

export default { preFilterNews };
