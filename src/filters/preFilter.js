/**
 * AI 분석 전 사전 필터링
 * 1. 광고/무관 패턴 제외
 * 2. 주요 언론사 필터
 */

// ==================== 제외 패턴 ====================
const EXCLUDE_PATTERNS = [
    // 광고/홍보
    /\[광고\]/i, /\[AD\]/i, /\[PR\]/i, /\[후원\]/i, /\[협찬\]/i,
    /\[제휴\]/i, /\[파트너\]/i, /sponsored/i, /advertisement/i,
    /브랜디드/, /네이티브 광고/, /제휴콘텐츠/,

    // 이벤트/프로모션/쇼핑
    /이벤트 참여/, /경품 응모/, /할인 쿠폰/, /세일 기간/, /특가 행사/, /무료체험/,
    /응모하/, /당첨자/, /추첨/, /증정/, /사은품/, /프로모션 행사/,
    /쇼핑몰/, /최저가 구매/, /핫딜/, /타임세일/,
    /블랙프라이데이/, /사이버먼데이/, /빅세일/,

    // 연예/엔터테인먼트
    /드라마 출연/, /예능 프로그램/, /아이돌 그룹/, /걸그룹/, /보이그룹/, /가수 활동/,
    /배우 출연/, /연기자/, /연기력/, /연기 활동/, /연예인/, /셀럽/, /스타 화보/, /팬덤/,
    /콘서트 개최/, /팬미팅/, /앨범 발매/, /음원 차트/, /컴백 무대/,
    /뮤직비디오/, /뮤비 공개/, /티저 영상/, /공개방송/,
    /영화 개봉/, /박스오피스/, /관객수 돌파/,
    /웹툰 연재/, /웹소설/, /만화책/,

    // 스포츠
    /축구 경기/, /야구 경기/, /농구 경기/, /배구 경기/, /골프대회/, /테니스 대회/,
    /올림픽 출전/, /월드컵/, /프로리그/, /경기 결과/, /승리를 거/, /패배를 당/,
    /스포츠 선수/, /야구 선수/, /축구 선수/, /감독 선임/, /코치 영입/, /선수 이적/, /트레이드/,
    /e스포츠 대회/, /게임대회/, /롤드컵/, /LCK 결승/,

    // 미디어/포토/영상
    /포토뉴스/, /화보 촬영/, /움짤/, /직캠 영상/, /영상뉴스/,
    /\[포토\]/, /\[영상\]/, /\[움짤\]/, /\[화보\]/,
    /포토 갤러리/, /비하인드 컷/, /메이킹 필름/,

    // 날씨/운세/라이프
    /오늘의 운세/, /오늘의 날씨/, /주간 날씨/, /일기예보/,
    /별자리 운세/, /타로 점/, /사주팔자/, /궁합 보기/,
    /맛집 추천/, /레시피 공개/, /요리 방법/, /카페 추천/, /맛집탐방/,
    /여행지 추천/, /호텔 예약/, /숙박 할인/, /관광 명소/, /휴가 계획/,
    /패션 스타일/, /뷰티 팁/, /화장품 추천/, /메이크업 튜토리얼/, /헤어 스타일/,
    /다이어트 방법/, /피트니스 팁/,

    // 부동산/개인금융
    /아파트 분양/, /청약 일정/, /전세 시세/, /월세 가격/, /부동산 매매/,
    /대출 금리 비교/, /예금 금리/, /적금 추천/, /개인 재테크/,
    /신용카드 추천/, /카드 혜택/, /포인트 적립/, /마일리지 사용/,

    // 정치/사회 일반
    /국회 본회의/, /여당 의원/, /야당 대표/, /대통령 발언/, /총리 후보/, /장관후보자/,
    /선거 유세/, /투표 결과/, /공천 경쟁/, /정당 활동/, /당대표 선출/,
    /사건사고 현장/, /범죄 수사/, /검찰 기소/, /경찰 조사/, /재판 결과/, /법원 판결/,
    /시위 현장/, /집회 참가/, /농성 돌입/, /파업 돌입/,

    // 교육/입시
    /수능 시험/, /대입 전형/, /입시 설명회/, /학원 수강/, /과외 모집/,
    /대학 합격/, /등록금 인상/, /장학금 수혜/,

    // 건강/의료 일반
    /건강 관리 팁/, /질병 예방/, /치료 방법/, /병원 진료/, /처방전/,
    /다이어트 식단/, /살빼기 비법/, /체중 감량/, /비만 관리/,

    // 기타 무관
    /단독 인터뷰/, /칼럼니스트/, /사설/, /기고문/, /독자투고/,
    /부고 소식/, /조의/, /별세 소식/, /타계하/,
    /복권 당첨/, /로또 번호/, /당첨번호 공개/
];

// ==================== 주요 경제 언론사 및 통신사 ====================
const MAJOR_SOURCES = [
    // 1순위 - 주요 경제지
    'hankyung.com', '한국경제', '한경',
    'mk.co.kr', '매일경제', '매경',
    'sedaily.com', '서울경제',
    'yonhapnews', 'yna.co.kr', '연합뉴스',

    // 2순위 - IT/산업 전문
    'etnews.com', '전자신문',
    'thelec.kr', '더일렉',
    'theelec.kr',
    'mt.co.kr', '머니투데이',
    'asiae.co.kr', '아시아경제',
    'biz.chosun.com', '조선비즈',

    // 3순위 - 주요 일간지 및 통신사
    'chosun.com', '조선일보',
    'donga.com', '동아일보',
    'joongang.co.kr', '중앙일보',
    'hani.co.kr', '한겨레',
    'khan.co.kr', '경향신문',
    'news1.kr', '뉴스1',
    'newsis.com', '뉴시스'
];

// ==================== 해외 주요 언론사 (글로벌) ====================
const MAJOR_GLOBAL_SOURCES = [
    // Finance & Biz
    'Bloomberg', 'Reuters', 'Wall Street Journal', 'WSJ',
    'Financial Times', 'FT.com', 'CNBC', 'Forbes', 'Fortune',
    'Business Insider', 'MarketWatch', 'Barron', 'Investopedia',
    'The Economist', 'Yahoo Finance',

    // Tech
    'TechCrunch', 'The Verge', 'Wired', 'VentureBeat',
    'Engadget', 'CNET', 'ZDNet', 'Ars Technica',
    'Tom\'s Hardware', 'AnandTech',

    // General Major
    'New York Times', 'NYT', 'Washington Post', 'BBC', 'CNN',
    'Guardian', 'Associated Press', 'AP News'
];

/**
 * 제외 패턴 체크
 */
function hasExcludePattern(text) {
    return EXCLUDE_PATTERNS.some(pattern => pattern.test(text));
}

/**
 * 주요 언론사 체크 (국내/해외 통합)
 */
function isMajorSource(item) {
    const link = item.link || '';
    const publisher = item.publisher || '';
    const lowerLink = link.toLowerCase();
    const lowerPublisher = publisher.toLowerCase();

    // 1. 글로벌 뉴스인 경우: 해외 주요 언론사 체크
    if (item.isGlobal) {
        return MAJOR_GLOBAL_SOURCES.some(source =>
            lowerPublisher.includes(source.toLowerCase()) ||
            lowerLink.includes(source.toLowerCase())
        );
    }

    // 2. 국내 뉴스인 경우: 국내 주요 언론사 체크
    if (publisher) {
        if (MAJOR_SOURCES.some(source => lowerPublisher.includes(source.toLowerCase()))) {
            return true;
        }
    }

    // link 도메인 확인
    return MAJOR_SOURCES.some(source => lowerLink.includes(source.toLowerCase()));
}

/**
 * 사전 필터링 메인 함수
 */
export function preFilterNews(newsItems) {
    const passed = [];
    const excluded = {
        adPattern: 0,
        notMajorSource: 0
    };

    for (const item of newsItems) {
        const fullText = `${item.title} ${item.description || ''}`;

        // 1. 광고/무관 패턴 체크
        if (hasExcludePattern(fullText)) {
            excluded.adPattern++;
            continue;
        }

        // 2. 주요 언론사 체크
        if (!isMajorSource(item)) {
            excluded.notMajorSource++;
            continue;
        }

        // 3. 통과
        passed.push({
            ...item,
            isMajorSource: true
        });
    }

    const globalCount = passed.filter(n => n.isGlobal).length;
    const domesticCount = passed.length - globalCount;

    console.log(`🎯 [사전 필터] ${newsItems.length}개 → ${passed.length}개 통과`);
    console.log(`   ├─ 광고/무관 제외: ${excluded.adPattern}개`);
    console.log(`   ├─ 비주류 언론사 제외: ${excluded.notMajorSource}개`);
    console.log(`   └─ 통과 (국내: ${domesticCount}, 해외: ${globalCount})`);

    return passed;
}

export default { preFilterNews };
